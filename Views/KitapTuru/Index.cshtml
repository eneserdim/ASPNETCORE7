@model List<KitapTuru>


@if (TempData["basarili"] != null)
{
    <div class="alert alert-info" role="alert">
        <strong>@TempData["basarili"]</strong>
    </div>
}

<div class="container">


    <div class="row pt-3 pb-4">
        <div>
            <h1 style="text-align:center">Kitap Türleri</h1>
        </div>
    </div>



    <table class="table table-bordered" style="border-radius:50px;">
        <thead>
            <tr>
                <th scope="col">ID</th>
                <th scope="col">Kitap Türü</th>
            

            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <th scope="row">@item.Id</th>
                    <td>@item.Ad</td>
                    <th scope="col">

                        <a asp-controller="KitapTuru" asp-action="Guncelle" asp-route-id="@item.Id" class="btn btn-primary" type="button" style="width:250px">Güncelle</a>
                        <a asp-controller="KitapTuru" asp-action="Sil" class="btn btn-danger" asp-route-id="@item.Id" type="button" style="width:250px">Sil</a>

                    </th>


                

                </tr>
            }

        </tbody>
    </table>

    <a asp-controller="KitapTuru" asp-action="Ekle" class="btn btn-success btn-lg btn-block" type="button">Yeni Kitap Türü Ekle</a>

</div>

